<% page_title( t("user_account") )%>        

<table>
  <tr>
    <th><%= t("email") %></th>
    <td>
      <%= @user.email %>
    </td>
  </tr>  
  <tr>
    <th><%= t("roles") %></th>
    <td>
      <%= list_roles(@user) %>
    </td>
  </tr>
</table>

<p>
  <%= link_to_edit @user %> <%= t('or') %>
  <%= link_to t('back'), collection_url %>
</p>
